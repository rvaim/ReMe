# Attribution

This is an independent community plugin and is not an official AgentScope/ReMe or OpenAI release.

The ReMe MCP transport, Claude Code `auto_memory_cc` workflow, recall-tool model, and best-effort hook behavior are adapted from the Apache-2.0 licensed AgentScope ReMe project.

The dual-manifest and separate-hook-file layout follows the public design principles documented by `rvaim/rvaim-marketplace` under `plugins/plugin-creator`.
